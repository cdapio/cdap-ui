import { makeStyles } from '@material-ui/core/styles';

export const useWelcomeCardComponentStyles = makeStyles(() => ({
  welcomeText: {
    fontSize: '24px',
    fontWeight: 500,
    height: '36px',
  },
}));
