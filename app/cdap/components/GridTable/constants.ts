import T from 'i18n-react';

export const MISSING_NULL = 'Missing/Null';

const PREFIX = 'features.WranglerNewUI.GridPage.transformationUI.format.options.concatenate.labels';
export const ADD = `${T.translate(`${PREFIX}.add`).toString()}`;
export const ENTER_STRING = `${T.translate(`${PREFIX}.enterString`).toString()}`;
export const COPY_TO_NEW_COLUMN = `${T.translate(`${PREFIX}.copyToNewColumn`).toString()}`;
export const DESTINATION_COLUMN = `${T.translate(`${PREFIX}.destinationColumn`).toString()}`;
